return {
    x_fine_adjust = 1.7,    -- Tag horizontal position adjustment
    hide_x1 = true,         -- Hide "x1" text
    hide_text = false,      -- Hide all text
    text_anchor = 2,        -- Location of text; 1 = Left, 2 = Right
    autosave = false,       -- Save when combining tags
    save_tag_min_level = 2, -- Save when generating tags at or above this level
    debug = false,          -- Enable diagnostics

    -- Experimental configuration options
    fast_combine = false,   -- Perform Cat Tag combination faster
    auto_combine = false,   -- (Dangerous!) auto-combine Cat Tags

}
